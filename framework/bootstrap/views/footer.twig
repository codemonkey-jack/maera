{#<?php#}
{{ framework.open_container( 'div', 'wrap-main-section', 'wrap main-section' ) }}
	{% do action( 'maera/footer/content' ) %}



	{% if theme_mods.footer_social_toggle == 1 %}
		{% set social = true %}
	{% else %}
		{% set social = false %}
	{% endif %}

	{% set social_width = theme_mods.footer_social_width %}
	{% set width = 12 - social_width %}

	{% if theme_mods.footer_social_new_window_toggle == 1 %}
		{% set blank = ' target="_blank"' %}
	{% else %}
		{% set blank = null %}
	{% endif %}

	{{ framework.open_container( 'div', 'footer-copyright' ) }}
		{{ framework.open_row( 'div' ) }}
			{{ framework.open_col( 'div', 'large', 'copyright-bar' ) }}
				{{ theme_mods.footer_text|replace({'[year]': now|date('Y') ,'[sitename]': bloginfo('name') }) }}
			{{ framework.close_col( 'div' ) }}
			{{ framework.clearfix() }}
		{{ framework.close_row( 'div' ) }}
	{{ framework.close_container( 'div' ) }}

{{ framework.close_container( 'div' ) }}
