{#<?php#}
{% extends "base.twig" %}

{% block content %}

	{% do action( 'shoestrap/index/begin' ) %}

	{% for post in posts %}
		{% do action( 'shoestrap/in_loop/begin' ) %}
		{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
		{% do action( 'shoestrap/in_loop/end' ) %}
	{% endfor %}

	{% do action( 'shoestrap/index/end' ) %}

{% endblock %}
