{% import 'grid_classes.html.twig' as grid %}
{% macro container_open(element, id, extra_classes, properties) %}
	<{{ element|default('div') }} class="{{ grid.container()|apply_filters( 'maera/container_class' ) }}{{ extra_classes ? ' '~extra_classes : '' }}"{{ id ? ' id="'~id~'"' : '' }}{{ properties ? ' '~properties : '' }}>
{% endmacro %}
